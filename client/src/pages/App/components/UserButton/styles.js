export const styles = theme => ({
    avatar: {
        background: theme.palette.grey["800"],
    },
    userButton: {
        display: "flex",
        flexDirection: "row",
        alignItems: "center",
    },
    userNameDisplay: {
        color: theme.palette.common.white,
        fontSize: 16,
        marginRight: 12,
    },
});