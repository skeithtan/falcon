export const styles = theme => ({
    menuContainer: {
        maxWidth: 320,
    },

    extraPadding: {
        paddingTop: theme.spacing.unit * 3,
        paddingBottom: theme.spacing.unit * 3,
    },
});
