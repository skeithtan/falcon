extend type Query {
    subjects: [Subject]!
    academicYears: [AcademicYear]!
    classes(term: Int!, year: Int!): [Class]!

    faculties: [Faculty]!
    faculty(_id: ID!): Faculty!
    profileChangeRequests(facultyId: ID): [ChangeRequest!]!

    currentUser: User
    myProfile: Faculty!
    myChangeRequests: [ChangeRequest!]!
}
