input SubjectInput {
    code: String
    name: String
    major: String
}

extend type Mutation {
    createSubject(subject: SubjectInput): Subject!
    modifySubject(_id: String!, newSubject: SubjectInput): Subject!
}