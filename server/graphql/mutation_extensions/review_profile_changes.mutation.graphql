union FacultySubdocument = Degree | Recognition | Presentation | InstructionalMaterial | ExtensionWork

type RequestReviewAction {
    approve: FacultySubdocument!
    reject: Boolean!
}

extend type Mutation {
    reviewProfileChangeRequest(_id: ID!): RequestReviewAction!
}
